firebase-functions>=0.4.0
geoglows==2.2.0
# geoglows_reach_coords: read the metadata parquet + binary-search an in-memory
# index. geoglows already pulls these in transitively; pinned here so the
# coords function is explicit about what it needs.
numpy>=1.26
pandas>=2.0
pyarrow>=14.0
requests>=2.31
# geoglows_stream_conditions: read the daily forecast + return-period zarrs from
# GEOGLOWS S3 to classify each reach's flood category.
s3fs>=2024.2
zarr>=3,<4
