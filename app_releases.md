# App Releases

Release history for RIVR. Update this file whenever you bump the version or build number in `pubspec.yaml`.

## Versioning

`<year>.<major>.<minor>+<commit count>` — e.g. `2026.0.0+521`.

Stamp it with `make version` on `main` after merging, never by hand. The build
number is the repository's commit count, so it is derived rather than
remembered: there is no counter to forget, and it can only rise. Both stores
require a strictly increasing build number and reject a repeat, which is the
mistake this removes.

`YEAR`/`MAJOR`/`MINOR` are deliberate — MAJOR for a release users would notice,
MINOR for fixes, YEAR when the calendar does: `make version MAJOR=1`.

Adopted 2026-08-20, replacing semver `1.2.0+8`. Note this is one-way: both
stores enforce increasing versions, so there is no going back to `1.x`.

## Releases

| Version | Date | Commit | Summary |
|---------|------|--------|---------|
| 2026.2.3+862 | 2026-09-25 | — | **A river saved as a guest did not appear after signing in.** Pull-to-refresh did not bring it; deleting a different river did. The merge into the account runs AFTER `signIn` returns, by which time Firebase has switched the session and the auth stream has fired — so the page reloaded on that identity change, read the account's list **before the merge write landed**, and never reloaded again because the identity does not change twice. 857's fix keyed the reload on identity alone, which was already correct; the list was stale, not misattributed. `AuthProvider` now exposes a `dataRevision` bumped once a sign-in completes (the use case only returns after the merge is written), and `FavoritesProvider.ensureLoadedFor` is keyed on `(identity, revision)`, re-reading without blanking the list for the same person. A failed sign-in does not bump, so a typo costs nothing. Both halves mutation-checked. 1,445 tests. |
| 2026.2.3+857 | 2026-09-25 | — | **Signing in showed the guest's rivers, not the account's.** On 848 the account survived a guest sign-in (the 843 deletion defect is fixed) and the merge worked — the document held all 10 rivers, 8 existing plus the 2 saved as a guest — but the screen kept showing only the 2, and pull-to-refresh did not help. The data was never wrong: the LIST on screen belonged to the guest. The favourites list is loaded once from the page's `initState`, and `refreshAllFavorites` only refreshes the rivers already in it, so refreshing was faithfully refreshing the wrong two. `FavoritesProvider` now records which identity its list belongs to and exposes `ensureLoadedFor(uid)`, which reloads when the identity differs and returns immediately when it does not; the page subscribes to `AuthProvider` and calls it on every identity change, and signing out empties the list rather than leaving one person's rivers on screen for the next. **A regression caught by the existing integration suite, not by me:** routing the "Try Again" button through the new conditional path silently broke it, because after a failed load the identity has not changed and the path correctly does nothing — `favorites_flow_test.dart` failed immediately. Forced reload and identity-change reload are now separate, documented methods. Mutation-checked. 1,441 tests. |
| 2026.2.3+848 | 2026-09-25 | — | **Signing in from a guest DELETED YOUR ACCOUNT. The worst defect of the guest-mode work.** After a successful guest sign-in the code called `User.delete()` on the stale anonymous reference to tidy the orphan away — and deleted the user of the CURRENT session, the account that had just signed in. Jerson's real account `Jerson.7@icloud.com` (created 2026-02-22) was destroyed on 2026-09-22, along with `jersondevs@gmail.com`. **Proved rather than assumed:** the anonymous user survives in Auth while the account is gone, and the account's Firestore document survives orphaned with 6 favourites and 22 custom names, `updatedAt` two seconds after the guest merge — the app's own Delete Account removes the document first, so a user-initiated deletion would have taken both. The account was recovered by recreating the Auth user **with the same uid**, which reconnected the surviving document; nothing was lost. Fix: the sign-in path deletes nothing at all and `_deleteAbandonedGuest` is removed — there is no safe moment for it, since after success the guest is not the current user and before success nothing may be written. `guestGcDaily` reaps the orphan. **Structural guard:** `AuthFirebaseDatasource.deleteUser` now throws unless the target IS the current user, and the test fake mirrors the refusal so the guard is exercised. Mutation-checked. 1,438 tests. **Third build running broken by one habit** — an incidental write or delete placed where it looked harmless, turning out to be load-bearing (832 wrote before an operation that could fail; 838 let a best-effort write create the document a real write owned; 843 called a destructive API on a reference that no longer meant what it said). |
| 2026.2.3+843 | 2026-09-25 | — | **Guests could not save a single favourite in 838 — fixed.** Every stream returned "Failed to update favorites". The guest's Firestore document was a STUB holding only `favoriteReachIds`, `favoriteSources`, `lastActiveAt` and `updatedAt`, with **no `userId`** and none of the defaults. Cause: `touchLastActive` — the once-a-launch "sign of life" write that `guestGcDaily` depends on — used `set(merge: true)`, which CREATES. The auth-state listener fires the instant the anonymous user exists and calls it, so the stub landed first; `signInAnonymously` then saw a document already there and skipped writing the real settings. Reading it threw `type 'Null' is not a subtype of type 'String'` on the unconditional `userId` cast, and `FavoritesService.addFavorite` never got past `getUserSettings`. Three fixes: `touchLastActive` now uses `update` and cannot create anything; a document without `userId` is treated as a stub and filled in rather than trusted; and `UserSettingsDto.fromJson` takes the document id as a fallback and tolerates missing dates, because a missing field must degrade to a blank value, never to an exception that makes the app unusable. **Why the tests missed it:** every guest-mode test set itself up by calling `signInAnonymously()` directly, so none exercised the real startup ORDER where the listener races the settings write. A suite that always builds its own happy path cannot see an ordering defect — there is now a provider-level guard that drives `initialize()` and asserts on the resulting document. Four mutation checks. 1,437 tests. Second consecutive build broken by the same habit: a small, early, best-effort write that turned out to be load-bearing (838's was writing before an operation that could fail). |
| 2026.2.3+838 | 2026-09-22 | — | **Three defects from the first real run of guest mode on a phone, all found by Jerson on build 832 and all measured in Firebase rather than inferred.** (1) **A guest's saved rivers were destroyed by a failed sign-in.** The merge cleared favourites, sources, labels and push tokens BEFORE attempting the sign-in and restored them only in a `catch`; the restore did not run, and the document was left with `mergePending: true` and an empty list. Two real favourites lost, unrecoverable. Nothing is written now until the sign-in has succeeded — read, sign in, merge, delete — and `_neutraliseGuestDoc`, `_restoreGuestDoc` and `mergePending` are deleted outright. This was not a slip but a design that destroyed data first and repaired it over the network, at the moment the network is least reliable; it traded a cost problem (an orphan document driving alerts) for a data-loss problem. (2) **Linking left the account marked as a guest** with no email or name: `UserSettingsService` caches settings on the uid, and linking KEEPS the uid, so `syncAfterLogin` read the stale guest settings and wrote them back over the identity registration had just written. An account left with `isGuest: true` is, by `guestGcDaily`'s own rules, a deletion candidate once idle 90 days — spared only by the providers check. `syncAfterLogin` now invalidates the cache first, and Jerson's document was repaired by hand. (3) **"I've verified it" did nothing** — Firebase had the address verified the whole time, but the Account-page banner reads the cached `AuthUser`, which `checkEmailVerified` never refreshed. **The test that should have caught (1) passed**: it was named "a failed sign-in gives the guest their rivers back" and the fake threw cleanly so the restore path ran. The replacement asserts the stronger property — a failed or abandoned sign-in leaves the document byte-identical — which cannot pass while any pre-write exists. All three fixes mutation-checked. 1,430 tests. Also: the ADR 0014 Firestore rules had never been deployed (production still had the old permissive rule); deployed and verified live. Verification email delivery confirmed working for the first time since the sender was repaired on 2026-09-07. |
| 2026.2.3+832 | 2026-09-22 | — | **The app opens without an account.** App Review rejected 805 under Guideline 5.1.1(v): *"the app requires users to register before accessing map."* RIVR now signs people in anonymously on launch, and the map, forecasts, favourites, flood alerts and the Weekly Outlook all work with no personal information. An account is optional and only carries saved rivers to another device; it is offered once after the first favourite (dismissable) and on the Account page. **Creating an account LINKS the credential to the existing identity** rather than making a new one, so nothing migrates; signing in to an account you already have merges the guest's rivers into it, the account winning every conflict, and a failed sign-in restores the guest exactly. **Four things would have broken a guest silently and are fixed:** the email-verification screen would have locked every guest out (an anonymous user's `emailVerified` is always false), the `users/{uid}` document was only ever created by the register path, `deleteAccount` demanded a password a guest has no way to give, and Sign Out — which cannot be undone for an anonymous uid — is no longer offered to guests at all. Verification became an Account-page banner instead of a wall; the Terms/Privacy consent line moved to onboarding, the one screen every person now passes. New Cloud Function `guestGcDaily` deletes guests idle 90 days (Auth **and** document, so their reaches leave the store's refresh cycle), refuses a run that would delete more than half of them or more than 500, and can never select a user with a sign-in provider. ADR 0014. 1,426 Flutter tests and 468 Functions tests green, every critical path mutation-checked. **Two corrections:** `test/integration_test/` was recorded as carrying ~27 long-standing failures — it is GREEN, and this change produced exactly 27 new ones the stale note would have excused; and `firebase_auth_mocks`' `MockUser` defaults `isEmailVerified` to TRUE, which made two verification-gate guards vacuous until a mutation exposed them. **NOT yet run as a guest in a real app** — this Mac's Xcode has no Simulator.app or SimulatorKit, so the simulator cannot be driven; verification is this TestFlight build. |
| 2026.2.2+805 | 2026-09-07 | — | **Submitted to App Review 2026-09-07 and to Google Play review 2026-09-11 — the first submission to both stores.** **Android (2026-09-10):** Play refused build 800 on two counts — it targeted API 35 (Play now requires 36) and three native libraries were 4 KB-aligned (`libmapbox-maps.so`, `libmapbox-common.so`, `libc++_shared.so`; Flutter's own were fine). Fixed with `targetSdk = 36` and a one-step Mapbox bump, 2.11.0 → **2.12.0**, the first plugin release with 16 KB libraries (it pulls Mapbox Android 11.16.0). The first rebuild failed with 13 "unresolved reference" errors inside the plugin's own Kotlin — a stale incremental cache from 2.11.0, gone after clearing `build/mapbox_maps_flutter`. Verified from the AAB: versionCode 805, targetSdk 36, every arm64 and x86_64 library 16 KB-aligned; the 32-bit armeabi-v7a Mapbox libs remain 4 KB. 1,404 tests green. **Then the emulator found the real defect:** the map threw `MapboxConfigurationException` because `android/local.properties` had no `mapbox.token`, so Gradle injected the literal placeholder — build 800 had shipped to Play (draft) that way, and nothing in 1,404 tests or the artifact checks noticed. Token restored; `build.gradle.kts` now refuses any release build without a `pk.` token (mutation-checked: fails in 2 s with the line removed). **Verified on the fe4r_pixel emulator (Android 15):** sign-in, favourites with live data, Weekly Outlook, the map with streams drawn, location grant, tap-to-sheet (Mayfield Slough, 1.4 CFS, Normal). **iPhone only.** Add for Review refused build 800 because the binary declared iPad support (`TARGETED_DEVICE_FAMILY = "1,2"`) and Apple therefore required 13" iPad screenshots — for an app that has never been run on an iPad. Jerson's call: RIVR is not expected on iPad. The device family is now iPhone (`1`) in all three build configurations and the `~ipad` orientation block is gone from Info.plist. No other change since 800. |
| 2026.2.2+800 | 2026-09-07 | — | **The launcher icon on the home screen, and the App Store submission build.** The icon set shipped in 786 had the artwork sitting at 73% of the canvas inside a white square, which made RIVR the one visibly small icon on an iPhone home screen; every icon and splash asset is now generated from one full-bleed source by `scripts/generate_app_icons.py`, alpha-free for iOS and squircle-masked for Android, with the adaptive icon's artwork on the background layer so the launcher mask cannot crop the river. The Account page now shows the compiled version and build (`RIVR 2026.2.2 (800)`) read from the bundle, so the stamp cannot go stale. This is the first build meant for App Store review rather than TestFlight only: the listing copy, keywords, real privacy/support URLs and a reviewer account were prepared the same day (`release-assets/store-listing-template.md`). No data-path or notification changes since 786. |
| 2026.2.1+786 | 2026-08-30 | — | **ADR 0011 closed — all ten phases — plus two new ADRs.** The cloud store work is finished and the store has run in production since 2026-08-25. This build carries the Phase 9 sweep and everything after it. **Phase 9:** the NWM turns out to be four models on four schedules — short range publishes hourly for CONUS but every 6 hours for Puerto Rico and every 12 for Hawaii, and the islands have no medium or long range at all — so freshness windows, hold caps and product availability are now per-domain. The store product `analysisAssimilation` was renamed `currentFlow` because it never fetched analysis assimilation (it fetches short range) while NOAA returns a genuine section by that name; that collision had already caused three defects. 2,100 lines of dead code deleted. Two week-long false alarms silenced — 83 store errors that were four unfavourited reaches, and 103 push failures that were one debug build — which together were essentially the project's entire error volume for the week. Alerts no longer announce a crest that has already passed. **ADR 0012:** renaming a GEOGLOWS river is reversible again; the restore button was gated on a river name GEOGLOWS reaches never publish, and separately it offered to restore a name that was already the default. **ADR 0013:** the map centres at zoom 12 instead of 14 (the stream tileset is tiled to z12, so 14 stretched tiles and showed 1.6 km of ground instead of 6.4 km), shows Mapbox's location puck with its accuracy ring, centres on the first open only and then remembers where you left the camera for the rest of the session, and explains itself with an Open Settings dialog when location is refused instead of silently doing nothing. **Verified on the iPhone 17 Pro Max simulator:** GEOGLOWS rename and restore end to end, the location puck and zoom-12 framing over Provo, and the camera surviving a close-and-reopen. 1,398 Flutter tests and 454 Cloud Functions tests green. |
| 2026.2.0+719 | 2026-08-30 | 791b0e0 | **ADR 0011 Phases 4–8: rivers now come from one shared cloud store, and the app stopped guessing about freshness.** 157 commits, the first release since 2026-08-21. Seven Cloud Functions keep a Firestore `river_data` collection fresh for every favourited reach, so the app reads one shared value instead of every widget fetching its own — measured on device: favourites paint in **969 ms** cold on iPhone and **2473 ms** on an Android *emulator* (not hardware) against a 3 s bar, with **zero** upstream calls in a whole session, and they still render in airplane mode. Two accounts on two platforms show the same river identically (13966 CFS), reading a value the store holds once (the mechanism is inferred rather than instrumented — see ADR 0011 guard 3). Alerts moved onto the store and off the clock: they evaluate at the end of a refresh run only when a model run actually advanced, taking publication-to-alert from up to six hours to under one, and they now lead with the app's own flood category and the time to peak rather than a raw number. Per-river alert frequencies with entry/escalation/persistence/all-clear triggers replaced a single global setting. **Phase 7 removed every freshness timestamp from the value surfaces** — the "3h ago" line and, more importantly, the green tick claiming a value was current — leaving one unobtrusive indicator that appears only when the app genuinely cannot vouch for what it shows: offline, past a value's window with a failed refresh, or carrying a forecast run older than the server itself would stand behind. The client and server share those thresholds, pinned by a cross-language drift test. Also: custom favourite names now sync to Firestore so alerts use them; the favourites card no longer converts flood thresholds twice (which made almost every river read NORMAL for CFS users); GEOGLOWS values expire when GEOGLOWS actually publishes (10:15–10:30 UTC) rather than at midnight; stale push tokens are pruned instead of throwing every cycle; and weekly-digest-only users get notification tap routing. Four independent reviews of Phase 7, none of which passed on first reading — every round found real defects, recorded in ADR 0011 rather than smoothed over. 1301 app tests, 388 functions tests. **Not yet verified: end-to-end alert delivery**, which needs this build — local iOS builds cannot receive pushes because Firebase has no development APNs key. |
| 2026.1.3+561 | 2026-08-21 | — | **Weekly Outlook opened from a notification showed "no favorites" to accounts that had them.** With tap routing finally working in 555, the page it lands on rendered its empty state for an account with three favourites. A tap opens the page during launch, before `FavoritesProvider` has loaded from Firestore, and the page read favourites once via `context.read` — which does not subscribe — so it never recovered. It also returned early past `_recordOutlookOpen`, so the digest back-off counter never reset: tapping a digest moved the user *toward* biweekly throttling instead of away from it, since the counter only resets when the outlook is opened. The page now holds its loading state while favourites are still arriving and rebuilds when they land, with an 8-second floor so a genuinely empty account still gets the empty state. 7 guards, 6 of which were verified to fail against the previous behaviour. |
| 2026.1.2+555 | 2026-08-21 | — | **The actual cause of notification taps going nowhere: `firebase_messaging` predated UIScene support.** 2026.1.1+551 fixed a real defect (the missing `super` call) but the tap still landed on the last-viewed screen, cold or backgrounded. The app adopts the UIScene lifecycle — `Info.plist` sets `UISceneDelegateClassName = FlutterSceneDelegate` — while `firebase_messaging` was pinned at 16.0.3, which contains no `scene:` methods at all; scene delegate support landed in 16.1.0. Under UIScene the tap arrives through the scene delegate, so nothing was listening: the banner displayed, `willPresent` fired, and `onMessageOpenedApp` and `getInitialMessage` stayed silent. 16.5.0 implements `scene:willConnectToSession:` specifically to hand over the tapped notification. Confirmed upstream in flutter/flutter#185048, the identical configuration, closed by its reporter with the same diagnosis. Upgrades `firebase_messaging` 16.0.3 → 16.5.0 and `firebase_core` 4.2.0 → 4.13.0; `AppDelegate.swift` deliberately unchanged so the device test measures one variable. Collateral: mocks regenerated for the new `getToken` signature, and `fake_cloud_firestore` → 4.2.0 for `cloud_firestore` 6.8.0's generic `WriteBatch.update`. 866 tests green. Two earlier hypotheses — a plugin-registry split, and a dependency hijacking the notification-center delegate — were investigated and disproven; both are recorded in ADR 0008. |
| 2026.1.1+551 | 2026-08-21 | — | **Fixes notification tap routing.** Tapping the weekly digest returned the user to whatever screen they last had open instead of the Weekly Outlook page. `AppDelegate` overrode `userNotificationCenter(didReceive:)`, logged the payload and called `completionHandler()` itself without calling `super`; with `FirebaseAppDelegateProxyEnabled=false` there is no swizzling to pick that up, so FlutterFire never saw the tap, `onMessageOpenedApp` never fired, and the routing code — correct, and covered by six tests — was never invoked. Third defect from the same root: swizzling is disabled and the manual delegate wiring was incomplete. |
| 2026.1.0+547 | 2026-08-21 | — | **Notification permission lifecycle, phases 1–4 of ADR 0009.** A preference is account-level and syncs; a permission is device-level and does not. Nothing reconciled the two, so a device signing in to an opted-in account showed both toggles ON, registered a token, and had every message silently dropped by the OS — measured on Android with POST_NOTIFICATIONS denied. **Reconciliation:** opening notification settings now compares the account preference against this device's OS permission and asks if it has never been asked, at most once per session and never when denied. **Priming:** the first favourite now brings up a soft ask naming that river — "Get alerts for White River?" — with Enable / Not now, replacing a generic banner that linked to settings. "Not now" reaches the OS not at all, which matters because iOS grants one prompt per install and RIVR has no provisional fallback; it now expires after 30 days rather than never. **Denied state:** toggles render off and inert, the delivery schedule is hidden, and a blocked row routes to system settings; returning with permission granted registers the device with no further tap. A refusal on one device no longer misrepresents or affects another. **Service honesty:** enabling now reports tokenUnavailable instead of granted when no device token was obtained or the Firestore write failed, so a preference is never persisted for a device that cannot receive; token rotation adds before removing, so a failed write leaves a stale extra rather than no address at all. 44 new guard tests, 839 green. Also: Android build restored after the Flutter 3.47.1 upgrade (Gradle 8.14.3, AGP 8.11.1, Kotlin 2.2.20). |
| 2026.0.2+533 | 2026-08-21 | — | **The second half of the push-notification fix.** Nothing in the iOS project ever called `registerForRemoteNotifications()`. `Info.plist` sets `FirebaseAppDelegateProxyEnabled=false`, so Firebase does not swizzle the app delegate and will not register on the app's behalf; `AppDelegate` handled the token callback correctly but that callback only fires if registration was requested. Permission was granted, registration never started, `apnsToken` was never set, `getAPNSToken()` returned nil forever, and no FCM token was ever issued — the "Device not registered" state on every real device. The production entitlement shipped in 2026.0.1+529 was necessary but not sufficient: without this, no signing configuration could have produced a token. |
| 2026.0.1+529 | 2026-08-21 | — | **Fixes push notifications, which had never worked.** Build 524 shipped with `aps-environment=development`; Apple's production APNs refuses registration from such a build, so `getAPNSToken()` returned null forever, the APNs poll expired, and no FCM token was ever written — the app showed "Device not registered" and Firestore held tokens for 1 of 18 users, with `notification_logs` empty since launch. Root cause was a missing **Apple Distribution certificate for HydroMap LLC** (only Oqupa LLC's existed), so export had no distribution identity and fell back to the development team profile. Release now also carries its own `RunnerRelease.entitlements` declaring production, making the intent explicit; Debug and Profile keep development. Verified in the exported IPA: production entitlement, iOS Team Store Provisioning Profile, signed by Apple Distribution: HydroMap LLC. Also restores `NSLocationAlwaysAndWhenInUseUsageDescription` (ITMS-90683 on 524, a regression since 1.0.0+3), and adds `make upload-symbols` after the documented Crashlytics command turned out to be wrong for iOS. |
| 2026.0.0+524 | 2026-08-20 | — | First release since the Apple Developer account was recovered, and the first on the new `<year>.<major>.<minor>+<commits>` scheme. **Map:** rebuilt both stream networks as `-v3` tilesets — z0-12 with a stream-order ladder, fixing the broken low-zoom rendering, and tagging reaches whose geometry crosses lakes so a single Remote Config switch hides them on the base networks and the flood layer together. Flood colours now come from a daily pre-coloured tileset built by an autonomous Cloud Run job (classify → geometry join → NOAA merge → lake tagging → tiling → plausibility gates → Mapbox → Remote Config), which as of this release runs unattended at 11:00 UTC. The map opens on the user's location, or Provo when location is unavailable, instead of restoring an arbitrary last camera. Legend reduced to one line, "As of August 20th". **Detail sheet:** current flow and the forecast peak now sit side by side as equal tiles over the flood ladder, replacing a full-width flow card and a three-line text banner; reach metadata collapses behind a Details disclosure. **Flood colour:** one palette in AppConstants, indexed to the category ladder — fixes medium-range hourly bars that painted grey for every category above Normal because five widgets still switched on a retired vocabulary. Ink on filled swatches is now chosen by measured contrast (Action 1.6:1 → 8.3:1). **Offline:** connectivity is detected by reachability rather than by network interface, so a captive portal no longer reads as online; the banner is legible (2.2:1 → 9.6:1), announced to VoiceOver, and no longer covers the first favourite. The map shows its own notice explaining why new areas will not load. **Platform:** Flutter 3.47.1 (matching CI), iOS migrated to Swift Package Manager, seven Dependabot alerts closed in Cloud Functions. **Notifications:** Weekly Outlook digest deployed — Fridays 07:00 America/Denver. |
| 1.2.0+8 | 2026-07-11 | — | GEOGLOWS v2 integration: render GEOGLOWS streams on the map with a stream-source selector (NWM / GEOGLOWS outside-US / GEOGLOWS US-area, US-area OFF by default) and US-boundary mask; tap-to-source routing; GEOGLOWS forecast via a Cloud Function proxy with a minimal forecast page. River data layer SSOT refactor (ADR 0001): single `RiverDataRepository` with a shared `(source, reachId, product)` cache, publish-aligned TTL + stale-while-revalidate + in-flight dedup — fixes the tap→"See forecast" double-fetch; map bottom sheet (NWM + GEOGLOWS) and favorites read current flow through it. Integration test suite fully green (36/36). Cloud Functions transitive-dep security patch (protobufjs critical RCE, grpc-js highs). First main release to catch up with the 1.1.0+7 development line (account deletion, R8 hardening). |
| 1.1.0+7 | 2026-05-16 | e438854 | In-app account deletion (App Store Guideline 5.1.1(v)): Account page reachable from the three-dots menu with Delete Account at the bottom — reauth + Firestore `users/{uid}` cleanup + FCM token invalidation + biometric clear. Branded Android launch screen (parity with iOS, legacy + Android 12+ SplashScreen API). Dependabot: 1 critical + 6 high + 6 medium transitive vulns closed in Cloud Functions. iOS purpose strings, store-listing URLs, privacy-policy draft, Google Play feature graphic (from prior weeks, bundled in this build). |
| 1.1.0+6 | 2026-04-16 | — | Clean architecture rewrite (ServiceResult pattern, entity/DTO separation, layer-first folder structure, per-feature DI), disk cache with stale-while-revalidate, parallel non-blocking data loading, progressive loading with shimmer skeletons, GitHub Actions CI, Firebase Crashlytics and Analytics, offline connectivity banner, NOAA API retry logic, notification frequency settings, new app icons, UI refinements across all features, 50+ bug fixes |
| 1.0.0+5 | 2026-02-22 | — | Add favorites coach marks tutorial, smooth finger-tracking slide actions, pass current flow when adding favorites from map, change flow unit labels to ft³/s and m³/s, right-align settings menu icons, fix GlobalKey crash after coach marks navigation, fix video backgrounds randomly stopping |
| 1.0.0+4 | 2026-02-21 | — | Keep Standard basemap always light, fix stream colors |
| 1.0.0+3 | 2026-02-21 | 117eb48 | Add missing NSLocationAlwaysAndWhenInUseUsageDescription |
| 1.0.0+2 | 2026-02-21 | eb567ed | TestFlight internal testing build |
| 1.0.0+0 | 2026-02-20 | 0e2b450 | Initial release build for internal testing |
