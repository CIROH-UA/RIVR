# Cloud Functions Deploy History

Deployment history for RIVR Cloud Functions. Update this file whenever you deploy Cloud Functions to Firebase.

## Deployments

| Date | Commit | Files Changed | Summary |
|------|--------|---------------|---------|
| 2026-03-06 | 77b0cec | noaa-client.ts, notification-service.ts | Fix NOAA API response handling, improve error handling and return period cache fallback, fix unit conversion (CFS→CMS) in threshold comparison |
| 2026-03-27 | 031b3fa | .env | Rotate NWM API key after accidental exposure; new key provided by Ben Lee (CIROH) |
| 2026-07-11 | 26fd923 | package-lock.json | Security patch: `npm audit fix` on transitive deps (protobufjs 7.5.8→7.6.5 critical RCE/DoS, @grpc/grpc-js 1.14.3→1.14.4 two highs, form-data 2.5.5→2.5.6, qs, js-yaml). No source/behavior change; redeployed all 7 functions to run the patched deps. |
| 2026-07-15 | 56b01c0 | notification-service.ts, geoglows-client.ts (new) | Add GEOGLOWS flood alerts. Notification service now reads `users/{uid}.favoriteSources`, keys pre-fetched data by source+reachId (comid/linkno can collide), and branches NWM vs GEOGLOWS per favorite. New geoglows-client fetches the GEOGLOWS proxy once for forecast + gumbel return periods, converting median CMS→CFS so the shared evaluator works unchanged; `source` added to the FCM data payload so taps open the right forecast API. Redeployed all 7 default-codebase functions under jersondevs@gmail.com. Requires the `users` composite index (deployed via `firebase deploy --only firestore:indexes`). |
| 2026-08-11 | 3443a48 | functions_geoglows/main.py, requirements.txt | Daily flood-conditions precompute for GEOGLOWS. Four new functions in the `geoglows` codebase (all us-west1): `geoglows_conditions_refresh` (scheduled 11:00 UTC, fans out one Pub/Sub message per VPU), `geoglows_conditions_worker` (Pub/Sub, one region per instance — `concurrency=1` and `max_instances=20` after workers were killed at 4 GiB sharing instances), `geoglows_conditions_publish_global` (11:30 UTC, merges regions into one world file grouped by VPU), and `geoglows_conditions_latest` (HTTPS, `min_instances=1` to remove a ~7.5s cold start — costs roughly $3/month and needs `--force` to deploy). New private bucket `gs://ciroh-rivr-app-conditions`; the bucket cannot be public because an org policy forbids it, hence serving through a function. Also made the `geoglows` import lazy — at ~12s it exceeded the CLI's 10s budget for reading function signatures and blocked all deploys. Replaces on-demand computation that cost 15-300s per region and could not complete at all for the largest 14 VPUs (~33% of the world's rivers). Deployed under jersondevs@gmail.com.
| 2026-08-20 | (this deploy) | none — deploying existing source | Deploy `sendWeeklyOutlook` (1st Gen, Node 22, us-central1, 1GB/540s). The Weekly Outlook digest was written during the Apple account lockout and never deployed: `weekly-digest.ts` and its `index.ts` export existed in source, compiled, and were covered by tests, but no function or scheduler existed in the project, so the feature could not run. Deployed the single function rather than the whole codebase to avoid redeploying the six working flood-alert functions. Firebase created `firebase-schedule-sendWeeklyOutlook-us-central1` — `0 7 * * 5` America/Denver, ENABLED. Note zero users currently have `weeklyOutlookEnabled: true`, so the first runs will find no recipients until someone opts in. Deployed under jersondevs@gmail.com. |
| 2026-08-22 | (this deploy) | publish-cadence-probe.ts (new), index.ts, noaa-client.ts | **ADR 0011 Phase 0 instrumentation — not a notification change.** New 1st-gen function `probePublishCadence` (us-central1, 256MB/180s, hourly `0 * * * *` UTC) samples NOAA's unfiltered `/streamflow` for one reach and records each series' `referenceTime`, elapsed ms, bytes and HTTP status to a new `publish_cadence_log` collection. Deliberately does **no retries** — a retry would hide the failure rate being measured. Exists because every latency figure in ADR 0011 came from one sitting during a NOAA outage, and the probe interval, monitoring thresholds and cache policy all depend on knowing typical behaviour. Its first sample landed during a live full outage of `/streamflow` (all five series 504 while `/reaches` metadata, CIROH return periods and GEOGLOWS stayed up) and correctly recorded it as a failure. Redeployed the same day after review found that HTTP 200 with an empty `mediumRange` — also observed live — was being logged as healthy; `ok` now means "fetched and parsed" and `complete` means "every series present", with analysis keyed on the latter. `NOAA_CONFIG` exported from noaa-client so the probe stops hardcoding a third copy of the base URL. Ten unit tests added (`npm test`), three of which were demonstrated to fail against the pre-fix code. Deployed under jersondevs@gmail.com. |
| 2026-08-22 | (this deploy) | publish-cadence-probe.ts, publish-cadence-probe.test.ts | **ADR 0011 Phase 0 probe v2 — samples all five endpoints, not just the heaviest.** v1 measured only the unfiltered `/streamflow` response. A 50-sample comparison (10 rounds, all variants fired simultaneously) showed that is the wrong sensor: `analysis_assimilation` and `short_range` were 10/10 at ~2.1 s while unfiltered was 8/10 and `medium_range` 9/10 — so v1's failure rate described the worst case rather than the calls the app depends on. It also read `referenceTime` only from the unfiltered response, the one with the worst observed success rate, so a failure there lost the freshness signal entirely. v2 fires all five simultaneously each hour (keeping weight and bad-minute separable), writes one schema-versioned document per hour holding every result, takes `referenceTime` from the filtered calls, and cross-checks the unfiltered body against them. Hourly for all five including the 6-hourly series, because the open question is *when* NOAA publishes — the 00Z medium-range run was seen landing ~07:20Z, later than its own cycle — and a 6-hourly probe could only place that inside a 6-hour window. Still no retries. 13 unit tests. First live sample: 2/5, with only the two small endpoints answering. Deployed under jersondevs@gmail.com. |
| 2026-08-25 | (this deploy) | store-upstream.ts, store-service.ts, store-static.test.ts, index.ts, noaa-client.ts | **ADR 0011 Phase 5 server half — not a notification change.** New 1st-gen function `storeStaticDaily` (us-central1, 512MB/540s, `30 2 * * *` UTC), and `CAN_FETCH.nwm` extended with `reachMetadata` and `returnPeriods`, which also changes `storeRefreshHourly` and `storeWriteThroughOnFavourite`. Phase 5 review round 1 found guard 1 ("a favourite renders with ZERO upstream calls from the device") unreachable without them: every surface that renders a favourite reads the river's NAME and its flood THRESHOLDS, so with those two products missing each favourite still made two device-side calls. They are deliberately NOT on the hourly cycle — no run identity for the probe to compare, and a 30-day window — so `storeStaticDaily` refetches only what is missing or within 7 days of expiring. Both fetchers are local to store-upstream.ts rather than reusing `noaa-client`'s helpers, because those retry (forbidden for the store's fetchers) and `getReturnPeriods` falls back to a `return_period_cache` entry of any age, which would stamp an arbitrarily old value with a fresh 30-day window. **Verified by counting, not by exit status:** function count went 6 → 7 (`firebase functions:list`, only `storeStaticDaily` added), scheduler `firebase-schedule-storeStaticDaily-us-central1` ENABLED, and Firestore now holds 29 `reachMetadata` + 29 `returnPeriods` documents alongside the flow products (181 total), read back through the Firestore REST API rather than from the pipeline's own logs. The first run reported `ok` while failing 3 of 29 reaches — NOAA returns `name: ""` for some real reaches, and the fetcher rejected it, which would have failed those reaches every day forever and diverged from the live path (`ReachDataDto.fromNoaaApi` accepts `""`). Fixed and redeployed; the follow-up run wrote the remaining 3 with 0 failures and correctly skipped `returnPeriods` as "all current". Side effect worth noting: this deploy also CREATED `triggerWeeklyOutlook`, an HTTPS function that existed in source but had never been deployed. Deployed under jersondevs@gmail.com. |
| 2026-08-29 | (this deploy) | notification-service.ts, alert-triggers.ts, store-alert-source.ts, flow-classification.ts, weekly-digest.ts, index.ts | **ADR 0011 Phase 6 — alerts moved onto the store and off the clock.** Whole `default` codebase redeployed (12 functions). **Four functions DELETED:** `checkRiverAlerts6am`, `checkRiverAlerts12pm`, `checkRiverAlerts6pm`, `checkRiverAlerts12am` — Firebase refused the deploy until they were removed explicitly, and their Cloud Scheduler jobs went with them. Alerts are now evaluated at the end of `storeRefreshHourly` (:20) and `storeGeoglowsDaily` (11:30 UTC), only when a run actually advanced, which takes publication-to-alert from up to six hours to under one. Alerts and the weekly digest both read the Firestore store instead of calling NOAA/GEOGLOWS — `batchFetchReachData` is deleted, so no live-fetch path remains in the alert code (guard 1). Notification copy now leads with the app's own flood category and how soon the peak arrives; `SCALE_FACTOR` removed. **Verified by listing, not by the deploy's exit status:** `gcloud functions list` shows no `checkRiverAlerts*`, and the scheduler jobs are gone. A forced run confirmed the new path end to end — long range advanced 18:46, alerts evaluated 18:49, one notification sent (reach 620569308, logged `category: Action, trigger: entry`), where that reach had been sending four identical notifications a day for at least three days. **Two live defects found by independent review the next day and fixed in code, NOT yet deployed:** a river muted while calm could never escalate (the state that detects a rise was only written on a send), and a river flapping on its threshold could send 24 notifications a day (entry and all-clear consulted no interval). Deployed under jersondevs@gmail.com. |
| 2026-08-29 | 89a56b6, 5a97e61 | store-trigger.ts, store-service.ts, store-window.ts | **ADR 0011 Phase 7 monitoring — not a notification change.** Whole `default` codebase redeployed twice in one sitting. No functions added or removed; the change is inside `checkStoreHealth`, which backs `storeHealth` (HTTPS) and `storeHeartbeat` (2-hourly). Health was measured across the WHOLE `river_data` collection via `lastSuccessfulWrite`, so one fresh write reported the entire store healthy and a product that stopped being written was invisible. `assessProductFreshness` now judges the newest document per product against that product's `MAX_HOLD_MS`. **The first deploy false-alarmed within a minute:** `storeHealth` returned 503 `{"status":"down","problems":["returnPeriods has not advanced for 17h (cap 6h)","reachMetadata has not advanced for 17h (cap 6h)"]}` on a healthy store — the two near-static products have no refresh cycle, hold a 30-day window and are rewritten only when missing or nearly expiring, but had no `MAX_HOLD_MS` entry and inherited the 6-hour default meant for hourly forecasts. Both given 32-day caps and redeployed. **Verified by reading the structured log, not by exit status or by the endpoint's summary:** `storeHealth` returns 200 healthy AND its log line lists every product it actually checked — `analysisAssimilation:2h, shortRange:1h, mediumRange:9h, longRange:3h, geoglowsForecast:11h, returnPeriods:17h, reachMetadata:17h` — which is what distinguishes "healthy" from "healthy because it read nothing". **Known limit, found by review after deploy and NOT yet addressed:** this measures write recency, so it catches a refresher that stops writing but NOT one that writes stale content on time — which is the shape the 2026-08-29 GEOGLOWS incident actually had, so this would not have caught it. Run-currency checking (stored `runId` vs the probe's) is the next step. Also note `checkStoreHealth` now reads every stored document (~135-180 reads) per call instead of 2, roughly 3-4% of the free daily tier at the 2-hourly cadence, and `storeHealth` is public and unauthenticated. Deployed under jersondevs@gmail.com. |
| 2026-08-29 | df1db3f..643d374 | store-trigger.ts, store-window.ts, store-firestore.ts, store-service.ts, index.ts | **ADR 0011 Phase 7 run currency — not a notification change.** Whole `default` codebase redeployed; no functions added or removed. Adds `assessRunCurrency`, which measures how old the WATER is rather than how recently we wrote. The earlier per-product check measured write recency only, and would NOT have caught the 2026-08-29 GEOGLOWS incident: that job wrote punctually every day carrying a run one day old, so `fetchedAt` never aged. Caps are measured, not reasoned — replaying 163-168 `publish_cadence_log` samples gives worst observed run ages of 11.0h (analysisAssimilation), 8.0h (shortRange), 13.0h (mediumRange), 21.0h (longRange) against caps of 16/16/24/36h; the first attempt used 8h for the hourly pair and would have false-alarmed on 29 of 163 samples (17.8%). GEOGLOWS is 42h, from a production measurement rather than the publish schedule: the run check at 2026-08-29T11:30:34Z logged `held: 2026-08-28T00:00:00Z` against `upstream: 2026-08-29T00:00:00Z`, a 35.5h-old run replaced seconds later, so the 36h first attempt left ~25 minutes of margin. **Behaviour changes worth knowing:** `storeHealth` now returns **200 for `degraded`** and 503 only for `down` — one NOAA series pausing used to serve an outage page, because `analysisAssimilation` and `shortRange` come from the same series and each contributed two problems. Status is counted by CAUSE. Read `status`, never the HTTP code alone. Also `planWindowExtensions` now clamps to `fetchedAt + maxHold`, so an extension never promises past the hold cap. **Verified by reading the structured log, not the deploy's exit status:** `storeHealth` returns 200 healthy AND logs both dimensions — `products` (write age) and `runs` (run age) — for all seven products, which is what distinguishes "healthy" from "healthy because it read nothing". Three independent reviews ran; all three found blockers, all fixed. Deployed under jersondevs@gmail.com. |
| 2026-08-30 | 6cfe376 | notification-service.ts | **Stale FCM token cleanup — a real notification fix.** `FieldValue.arrayRemove` takes varargs and was being handed the array itself, so every cleanup threw `Element at index 0 is not a valid array element. Nested arrays are not supported.` No stale token had ever been pruned, and every alert cycle retried the same dead devices indefinitely. Recorded in ADR 0008 and scheduled for the Phase 9 sweep; found firing live while watching a real alert cycle for ADR 0011 Phase 8 guard 5 (the 03:20 UTC run logged four stale tokens and four cleanup failures). **VERIFIED at the next cycle (04:20 UTC), not by this deploy's exit status:** the account held 7 tokens before the deploy; the 04:20 run logged 9 prunes with zero cleanup errors and the document now holds 4. Also learned in the same run, and NOT a defect: local Xcode builds cannot receive alerts because Firebase has a Production APNs auth key (`P663R3US8S`) and no development one, so a sandbox-registered build fails with `messaging/third-party-auth-error`. TestFlight and App Store builds are unaffected. Deployed under jersondevs@gmail.com. |
| 2026-08-30 | b1cf108 | weekly-digest.ts | **Weekly digest windows to what is ahead — a real notification-content fix.** The digest's trend and peak were computed over the WHOLE stored series, including points already in the past, while the app windows to points still ahead. The trend rule itself was identical on both sides (`trendOf` even carries a comment saying so) — the inputs were not. Found from two screenshots a minute apart: the push said "7 rivers, 6 rising" and the page it opens said "5 rising · 2 steady/receding". Measured rather than reasoned: the first suspect (Diamond Creek) was disproven by computing both rules over its real stored series, then running the comparison across all seven of the user's favourites found exactly one disagreement — White River (18471070), rising to the server and falling to the app. The peak had the same flaw and is the worse half: over the whole series the digest can announce a crest that has already happened. **Verified by firing `triggerWeeklyOutlook` after deploy** — 2 users checked, 2 digests sent, 0 errors, and zero stale-token cleanup failures (the second confirmation of the `arrayRemove` fix, this time on the digest path). Note the notification BODY is not in the logs, so the corrected count is visible on device rather than server-side. Deployed under jersondevs@gmail.com. |
| 2026-08-30 | 150e245, 15cca95, 69d87bd, a04d761 | store-document.ts, store-window.ts, store-service.ts, store-trigger.ts, store-firestore.ts, store-upstream.ts, store-keys.ts, publish-cadence-probe.ts, notification-service.ts | **Three deploys in one sitting (15:39, 16:14, 16:24 UTC), all `functions:default`, no functions added or removed — count stayed 7 store functions, verified with `firebase functions:list` rather than from the deploys' exit status.** (1) **NWM domain schedules.** Short range publishes hourly for CONUS but every 6 hours for Puerto Rico and every 12 for Hawaii, measured by counting the `tNNz` run directories NOAA actually published on NOMADS; islands have no medium or long range at all. `validUntil` and the hold/run-age caps now take a `reachId`. Latent, not live — all 31 NWM reaches in the store are CONUS. (2) **`analysisAssimilation` renamed to `currentFlow`, a WIRE-FORMAT change.** Document ids move from `nwm__<reach>__analysisAssimilation` to `nwm__<reach>__currentFlow`, orphaning 31 documents (19% of the collection, under `assertGcSane`'s 50% ceiling) which `storeGcDaily` sweeps after its 7-day grace. **The installed TestFlight build 2026.2.0+719 will miss on this product and fall through to the live path for current flow until a new build ships** — one upstream call per favourite instead of zero. The old name never fetched analysis assimilation (it calls `fetchForecast(reachId, 'short_range')`) while NOAA returns a genuine `analysisAssimilation` section the app also consumes; that collision had already caused review rounds 2 and 3 and an island-schedule regression the same morning. `publish-cadence-probe.ts` deliberately still uses the name, where it is NOAA's. (3) **Two week-long false alarms silenced, and this is the notification-relevant part.** 103 send failures in seven days were all `messaging/third-party-auth-error` from ONE user — a debug build registered against Apple's sandbox, which this project cannot reach because it has a Production APNs key and no development one. It is now a WARN naming the cause, and a per-run count raises ERROR only when **two or more different users** fail the same way, which is the production key being broken and would silently kill every notification the app sends. **It must never be pruned:** the error reports our credential, not a dead token, so pruning on it would erase every push token in the system on a config mistake. Separately, 83 store errors were four unfavourited reaches whose documents no run rewrites, reported hourly as "upstream has gone quiet"; `planWindowExtensions` now counts orphans separately. Together those two were essentially the project's entire ERROR volume for the week. **VERIFIED at the 17:20 UTC run**, counted rather than taken from a success message: 27 `currentFlow` documents against a work list of exactly 27 favourited NWM reaches (every one matched), zero store ERRORs in that run against one every hour for the preceding week, and the APNs failures now reading as `📵 APNs credential does not cover this token's environment … expected for a debug build` at WARN while the run completed alerts and sent notifications normally. Note the expected count of 31 taken from the old documents was WRONG — those included four orphaned reaches nobody favourites, which no run rewrites; 27 is right, and deriving the expected number from the work list rather than from the previous count is what turned an apparent 4-document gap into an exact match. The 16:20 run before it correctly did nothing, because NOAA returned HTTP 504 on all five probe endpoints at 16:00. Deployed under jersondevs@gmail.com. |
