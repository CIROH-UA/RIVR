# Cloud Functions Deploy History

Deployment history for RIVR Cloud Functions. Update this file whenever you deploy Cloud Functions to Firebase.

## Deployments

| Date | Commit | Files Changed | Summary |
|------|--------|---------------|---------|
| 2026-03-06 | 77b0cec | noaa-client.ts, notification-service.ts | Fix NOAA API response handling, improve error handling and return period cache fallback, fix unit conversion (CFS→CMS) in threshold comparison |
| 2026-03-27 | 031b3fa | .env | Rotate NWM API key after accidental exposure; new key provided by Ben Lee (CIROH) |
| 2026-07-11 | 26fd923 | package-lock.json | Security patch: `npm audit fix` on transitive deps (protobufjs 7.5.8→7.6.5 critical RCE/DoS, @grpc/grpc-js 1.14.3→1.14.4 two highs, form-data 2.5.5→2.5.6, qs, js-yaml). No source/behavior change; redeployed all 7 functions to run the patched deps. |
| 2026-07-15 | 56b01c0 | notification-service.ts, geoglows-client.ts (new) | Add GEOGLOWS flood alerts. Notification service now reads `users/{uid}.favoriteSources`, keys pre-fetched data by source+reachId (comid/linkno can collide), and branches NWM vs GEOGLOWS per favorite. New geoglows-client fetches the GEOGLOWS proxy once for forecast + gumbel return periods, converting median CMS→CFS so the shared evaluator works unchanged; `source` added to the FCM data payload so taps open the right forecast API. Redeployed all 7 default-codebase functions under jersondevs@gmail.com. Requires the `users` composite index (deployed via `firebase deploy --only firestore:indexes`). |
| 2026-08-11 | 3443a48 | functions_geoglows/main.py, requirements.txt | Daily flood-conditions precompute for GEOGLOWS. Four new functions in the `geoglows` codebase (all us-west1): `geoglows_conditions_refresh` (scheduled 11:00 UTC, fans out one Pub/Sub message per VPU), `geoglows_conditions_worker` (Pub/Sub, one region per instance — `concurrency=1` and `max_instances=20` after workers were killed at 4 GiB sharing instances), `geoglows_conditions_publish_global` (11:30 UTC, merges regions into one world file grouped by VPU), and `geoglows_conditions_latest` (HTTPS, `min_instances=1` to remove a ~7.5s cold start — costs roughly $3/month and needs `--force` to deploy). New private bucket `gs://ciroh-rivr-app-conditions`; the bucket cannot be public because an org policy forbids it, hence serving through a function. Also made the `geoglows` import lazy — at ~12s it exceeded the CLI's 10s budget for reading function signatures and blocked all deploys. Replaces on-demand computation that cost 15-300s per region and could not complete at all for the largest 14 VPUs (~33% of the world's rivers). Deployed under jersondevs@gmail.com.
| 2026-08-20 | (this deploy) | none — deploying existing source | Deploy `sendWeeklyOutlook` (1st Gen, Node 22, us-central1, 1GB/540s). The Weekly Outlook digest was written during the Apple account lockout and never deployed: `weekly-digest.ts` and its `index.ts` export existed in source, compiled, and were covered by tests, but no function or scheduler existed in the project, so the feature could not run. Deployed the single function rather than the whole codebase to avoid redeploying the six working flood-alert functions. Firebase created `firebase-schedule-sendWeeklyOutlook-us-central1` — `0 7 * * 5` America/Denver, ENABLED. Note zero users currently have `weeklyOutlookEnabled: true`, so the first runs will find no recipients until someone opts in. Deployed under jersondevs@gmail.com. |
| 2026-08-22 | (this deploy) | publish-cadence-probe.ts (new), index.ts, noaa-client.ts | **ADR 0011 Phase 0 instrumentation — not a notification change.** New 1st-gen function `probePublishCadence` (us-central1, 256MB/180s, hourly `0 * * * *` UTC) samples NOAA's unfiltered `/streamflow` for one reach and records each series' `referenceTime`, elapsed ms, bytes and HTTP status to a new `publish_cadence_log` collection. Deliberately does **no retries** — a retry would hide the failure rate being measured. Exists because every latency figure in ADR 0011 came from one sitting during a NOAA outage, and the probe interval, monitoring thresholds and cache policy all depend on knowing typical behaviour. Its first sample landed during a live full outage of `/streamflow` (all five series 504 while `/reaches` metadata, CIROH return periods and GEOGLOWS stayed up) and correctly recorded it as a failure. Redeployed the same day after review found that HTTP 200 with an empty `mediumRange` — also observed live — was being logged as healthy; `ok` now means "fetched and parsed" and `complete` means "every series present", with analysis keyed on the latter. `NOAA_CONFIG` exported from noaa-client so the probe stops hardcoding a third copy of the base URL. Ten unit tests added (`npm test`), three of which were demonstrated to fail against the pre-fix code. Deployed under jersondevs@gmail.com. |
| 2026-08-22 | (this deploy) | publish-cadence-probe.ts, publish-cadence-probe.test.ts | **ADR 0011 Phase 0 probe v2 — samples all five endpoints, not just the heaviest.** v1 measured only the unfiltered `/streamflow` response. A 50-sample comparison (10 rounds, all variants fired simultaneously) showed that is the wrong sensor: `analysis_assimilation` and `short_range` were 10/10 at ~2.1 s while unfiltered was 8/10 and `medium_range` 9/10 — so v1's failure rate described the worst case rather than the calls the app depends on. It also read `referenceTime` only from the unfiltered response, the one with the worst observed success rate, so a failure there lost the freshness signal entirely. v2 fires all five simultaneously each hour (keeping weight and bad-minute separable), writes one schema-versioned document per hour holding every result, takes `referenceTime` from the filtered calls, and cross-checks the unfiltered body against them. Hourly for all five including the 6-hourly series, because the open question is *when* NOAA publishes — the 00Z medium-range run was seen landing ~07:20Z, later than its own cycle — and a 6-hourly probe could only place that inside a 6-hour window. Still no retries. 13 unit tests. First live sample: 2/5, with only the two small endpoints answering. Deployed under jersondevs@gmail.com. |
| 2026-08-25 | (this deploy) | store-upstream.ts, store-service.ts, store-static.test.ts, index.ts, noaa-client.ts | **ADR 0011 Phase 5 server half — not a notification change.** New 1st-gen function `storeStaticDaily` (us-central1, 512MB/540s, `30 2 * * *` UTC), and `CAN_FETCH.nwm` extended with `reachMetadata` and `returnPeriods`, which also changes `storeRefreshHourly` and `storeWriteThroughOnFavourite`. Phase 5 review round 1 found guard 1 ("a favourite renders with ZERO upstream calls from the device") unreachable without them: every surface that renders a favourite reads the river's NAME and its flood THRESHOLDS, so with those two products missing each favourite still made two device-side calls. They are deliberately NOT on the hourly cycle — no run identity for the probe to compare, and a 30-day window — so `storeStaticDaily` refetches only what is missing or within 7 days of expiring. Both fetchers are local to store-upstream.ts rather than reusing `noaa-client`'s helpers, because those retry (forbidden for the store's fetchers) and `getReturnPeriods` falls back to a `return_period_cache` entry of any age, which would stamp an arbitrarily old value with a fresh 30-day window. **Verified by counting, not by exit status:** function count went 6 → 7 (`firebase functions:list`, only `storeStaticDaily` added), scheduler `firebase-schedule-storeStaticDaily-us-central1` ENABLED, and Firestore now holds 29 `reachMetadata` + 29 `returnPeriods` documents alongside the flow products (181 total), read back through the Firestore REST API rather than from the pipeline's own logs. The first run reported `ok` while failing 3 of 29 reaches — NOAA returns `name: ""` for some real reaches, and the fetcher rejected it, which would have failed those reaches every day forever and diverged from the live path (`ReachDataDto.fromNoaaApi` accepts `""`). Fixed and redeployed; the follow-up run wrote the remaining 3 with 0 failures and correctly skipped `returnPeriods` as "all current". Side effect worth noting: this deploy also CREATED `triggerWeeklyOutlook`, an HTTPS function that existed in source but had never been deployed. Deployed under jersondevs@gmail.com. |
| 2026-08-29 | (this deploy) | notification-service.ts, alert-triggers.ts, store-alert-source.ts, flow-classification.ts, weekly-digest.ts, index.ts | **ADR 0011 Phase 6 — alerts moved onto the store and off the clock.** Whole `default` codebase redeployed (12 functions). **Four functions DELETED:** `checkRiverAlerts6am`, `checkRiverAlerts12pm`, `checkRiverAlerts6pm`, `checkRiverAlerts12am` — Firebase refused the deploy until they were removed explicitly, and their Cloud Scheduler jobs went with them. Alerts are now evaluated at the end of `storeRefreshHourly` (:20) and `storeGeoglowsDaily` (11:30 UTC), only when a run actually advanced, which takes publication-to-alert from up to six hours to under one. Alerts and the weekly digest both read the Firestore store instead of calling NOAA/GEOGLOWS — `batchFetchReachData` is deleted, so no live-fetch path remains in the alert code (guard 1). Notification copy now leads with the app's own flood category and how soon the peak arrives; `SCALE_FACTOR` removed. **Verified by listing, not by the deploy's exit status:** `gcloud functions list` shows no `checkRiverAlerts*`, and the scheduler jobs are gone. A forced run confirmed the new path end to end — long range advanced 18:46, alerts evaluated 18:49, one notification sent (reach 620569308, logged `category: Action, trigger: entry`), where that reach had been sending four identical notifications a day for at least three days. **Two live defects found by independent review the next day and fixed in code, NOT yet deployed:** a river muted while calm could never escalate (the state that detects a rise was only written on a send), and a river flapping on its threshold could send 24 notifications a day (entry and all-clear consulted no interval). Deployed under jersondevs@gmail.com. |
