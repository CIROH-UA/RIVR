<?xml version="1.0" encoding="utf-8"?>
<!-- Launch splash screen (pre-API 31). Mirrors the iOS LaunchScreen:
     white background with the RIVR logo centered. Android 12+ uses the
     SplashScreen API instead — see values-v31/styles.xml. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/launch_background" />

    <item>
        <bitmap
            android:gravity="center"
            android:src="@drawable/launch_logo" />
    </item>
</layer-list>
