<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- No ic_launcher_background here any more: the adaptive icon's background
         layer is the artwork itself (mipmap-anydpi-v26/rivr.xml), not a colour. -->
    <!-- Launch / splash background. White to match the iOS LaunchScreen
         storyboard (which is white in both light and dark mode). -->
    <color name="launch_background">#FFFFFF</color>
</resources>
